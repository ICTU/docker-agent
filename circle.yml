machine:
  services:
    - docker

deployment:
  hub:
    tag: /.*/
    commands:
      - docker build -t ictu/docker-agent:${CIRCLE_TAG} .
      - MAJOR_VERSION=${CIRCLE_TAG/.*}; docker tag ictu/docker-agent:${CIRCLE_TAG} ictu/docker-agent:${MAJOR_VERSION}
      - MINOR_VERSION=${CIRCLE_TAG%.*}; docker tag ictu/docker-agent:${CIRCLE_TAG} ictu/docker-agent:${MINOR_VERSION}
